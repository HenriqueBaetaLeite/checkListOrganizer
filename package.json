{
  "name": "checklistorganizer",
  "description": "Create your own check list :)",
  "version": "1.0.0",
  "author": "Henrique Baêta Leite",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest",
    "test:node": "node --test src/tests/",
    "test:coverage": "jest --coverage",
    "drop": "npx sequelize db:drop",
    "create": "npx sequelize db:create",
    "migrate": "npx sequelize db:migrate",
    "seed": "npx sequelize db:seed:all",
    "reset": "npx sequelize db:drop && npx sequelize db:create && npx sequelize db:migrate",
    "stress": "artillery quick --count 10 -n 20 http://localhost:3001"
  },
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.9.3",
    "sequelize": "^6.37.2",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2",
    "supertest": "^6.3.4"
  }
}
